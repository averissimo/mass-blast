# Blast options and task
opts: "-dust no -max_target_seqs 500 -evalue 1E-20"
task: megablast
identity_threshold: .40 # value between 0 and 1

force_folder: test_blastn
clean_output: true

db:
  parent: "test/db"
  list:
    - blackberry

query:
  parent: "test/blastn"
  folders:
    - query
